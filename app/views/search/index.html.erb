search
<% @genres.each do |genre| %>
  <%= link_to genre.name, { :action => :index, :id => genre.name }, :class => 'label label-primary' %>
<% end %>

<% if @bands.present? %>
  <hr>
  <%= render partial: 'layouts/bands', locals: { bands: @bands } %>
<% end %>
